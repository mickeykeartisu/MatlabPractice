% ----------------------------------------------------------------------- %
% ----------------------------------------------------------------------- %
% プログラムの初期化
clear variables;  % 変数の初期化
clc % コマンドウィンドウの初期化

% ----------------------------------------------------------------------- %
% ----------------------------------------------------------------------- %
% ファイルを読み取る
[x, fs] = audioread("D:\名城大学\研究室\演習\M007_ATR503_A01_T01.wav");

% 分析を行う
F0 = Harvest(x, fs);    % 高さ
spectrogram = CheapTrick(x, fs, F0);    % スペクトログラム
AP = D4C(x, fs, F0);    % その他(非周期性指標)

% 変換
AP.f0 = AP.f0 * 1;
